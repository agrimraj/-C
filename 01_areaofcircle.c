//(The code calculating the area of a circle using radius)
#include <stdio.h>
int main(){
float radius;
   printf("Enter radius of circle\n");
   scanf("%f",&radius);
   printf("Area of circle is %f",3.14*radius*radius);
   return 0;
}